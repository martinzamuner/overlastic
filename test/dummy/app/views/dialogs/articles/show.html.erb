<h1>Article #<%= @article.id %></h1>

<p>
  <%= link_to_dialog "Edit (stack)", edit_dialogs_article_path(@article), overlay_args: { title: "Edit article" } %>
  -
  <%= link_to_dialog "Edit (replace_last)", edit_dialogs_article_path(@article), overlay_action: :replace_last, overlay_args: { title: "Edit article" } %>
</p>

<hr>

<p>
  <%= @article.body %>
</p>
